import React from 'react';
import {Container} from "react-bootstrap";

const ProductList = () => {
    return (
        <Container>
            Product List
        </Container>
    );
};

export default ProductList;